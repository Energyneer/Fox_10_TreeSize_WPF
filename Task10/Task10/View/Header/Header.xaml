<UserControl x:Class="Task10.View.Header.Header"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Task10.View.Header"
             mc:Ignorable="d">
    <Grid Height="50">
        <StackPanel Orientation="Horizontal">
            <local:Select DataContext="{Binding}"/>
            <local:SizeSelector DataContext="{Binding}"/>
            <local:Visibility DataContext="{Binding}"/>
            <local:CollapseAll DataContext="{Binding}"/>
        </StackPanel>
    </Grid>
</UserControl>
